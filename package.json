{
  "name": "usage-bar-ci",
  "version": "1.0.0",
  "description": "CI/CD tools for UsageBar",
  "private": true,
  "scripts": {
    "lint:actions": "for f in .github/workflows/*.yml; do echo \"Validating $f...\"; action-validator \"$f\"; done"
  },
  "devDependencies": {
    "@action-validator/cli": "^0.6.0"
  }
}
